import 'package:flutter/material.dart';

/// 사용자 관심사 클래스
class Interest {
  final String label;
  final IconData icon;
  final Color color;

  Interest({ required this.label, required this.icon, required this.color });
}